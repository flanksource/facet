Facet has been deployed.

Get the service URL:
  kubectl get svc {{ include "facet.fullname" . }} -n {{ .Release.Namespace }}

Port-forward to access locally:
  kubectl port-forward svc/{{ include "facet.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Health check:
  curl http://localhost:{{ .Values.service.port }}/healthz
