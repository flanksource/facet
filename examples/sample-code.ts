// Example TypeScript function for carbon screenshot testing
export function calculateFibonacci(n: number): number {
  if (n <= 1) return n;
  return calculateFibonacci(n - 1) + calculateFibonacci(n - 2);
}

export function greet(name: string): string {
  return `Hello, ${name}!`;
}
